version: "3.3"
services:
  xtbservice:
    build: .
    #image: ghcr.io/cheminfo-py/xtb-service:latest
    restart: always
    environment:
      - PORT=${PORT}
      - CACHEDIR=/ircache
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./ircache:/ircache
    security_opt:
      - seccomp:unconfined
